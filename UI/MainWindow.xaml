<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        xmlns:helpers="clr-namespace:UI.Helpers"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="项目路径:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding ProjectRootPath, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="0,0,10,0"/>
            <Button Content="开启监控" Command="{Binding StartMonitoringCommand}" Margin="0,0,5,0" Width="80"/>
            <Button Content="停止" Command="{Binding StopMonitoringCommand}" Margin="0,0,20,0" Width="60"/>
            <Button Content="添加演示卡片" Command="{Binding AddTestCardCommand}" Background="LightBlue" Margin="0,0,5,0"/>

            <Button Content="模拟 YJK 运行" 
            Command="{Binding SimulateYjkOutputCommand}" 
            Background="Orange" 
            Width="100"/>
        </StackPanel>

        <ItemsControl ItemsSource="{Binding DisplayCards}" Grid.Row="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10" Width="240" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="{Binding TargetBlockName}" FontWeight="Bold" Foreground="Blue"/>
                            <TextBlock Text="{Binding Mode}" FontSize="10" Foreground="Gray"/>
                            <Separator Margin="0,5"/>
                            <TextBlock Text="最新数据内容:"/>
                            <TextBlock Text="{Binding CurrentData.Rows[0][Key]}" FontSize="16" Foreground="DarkGreen" Margin="0,5"/>
                            <TextBlock Text="{Binding CurrentData.VersionId}" FontSize="9"/>
                            <DataGrid AutoGenerateColumns="False" 
          CanUserAddRows="False"
          IsReadOnly="True"
          Height="200"
          Margin="0,5"
          helpers:DataGridHelper.BindableColumns="{Binding CurrentData}">
                            </DataGrid>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</Window>
